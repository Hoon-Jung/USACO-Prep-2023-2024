fil = open("traffic.in")

#traffic flow before and after the data values

#??? + 1 car
#?? == 10, 14 - 1 each
#minimum value for two max values
#maximum value for two min values (the numbere representing lowest range)
#for getting off the range changes by (min - maxgetting off, max - min getting off)
#for getting on the range changes by (min + maxgetting, max + min getting aded)
#flip when calculating reverse
#get to last none before going back for getting the beginning 
before = []


# print(ans, file=open("traffic.out", "w"))